type Bridge {
  bridgeId: String!
  containerId: [String]
  accountId: String!
  username: String!
  namespace: String!
  directoryMap: String!
}

type Query {
  bridge (bridgeId: String!): Bridge,
  listBridgesByAccount: [Bridge]
}


type Mutation {
  createBridge (
    username: String!
    namespace: String!
    sshKey: String!
    accessKey: String!
    secretKey: String!
    directoryMap: String!
  ): Bridge,
  deleteBridge (bridgeId: String!): Boolean
}
